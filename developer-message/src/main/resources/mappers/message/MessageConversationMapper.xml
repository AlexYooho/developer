<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.developer.message.mappers.MessageConversationMapper">

    <resultMap id="BaseResultMap" type="com.developer.message.pojo.MessageConversationPO"/>

    <sql id="Base_Column_list">
        id, user_id, conv_type, target_id, last_msg_seq, last_msg_id, last_msg_content, last_msg_type, last_msg_time, unread_count, pinned, muted, deleted, draft, extra, create_time, update_time
    </sql>

    <select id="findList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_list"/>
            FROM message_conversation
        WHERE user_id = #{user_id}
        AND deleted = 0
        ORDER BY last_msg_time DESC
    </select>

    <update id="updateConversationInfo">
        UPDATE message_conversation
        SET last_msg_seq = #{po.lastMsgSeq},
            last_msg_id = #{po.lastMsgId},
            last_msg_content = #{po.lastMsgContent},
            last_msg_type = #{po.lastMsgType},
            last_msg_time = #{po.lastMsgTime},
            unread_count = #{po.unreadCount},
            pinned = #{po.pinned},
            muted = #{po.muted},
            deleted = #{po.deleted},
            draft = #{po.draft},
            update_time = #{po.updateTime}
        WHERE id = #{po.id}
    </update>

</mapper>